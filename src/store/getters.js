export const user = (state) => {
    return state.user;
}

export const authorized = (state) => {
    return state.authorized;
}

export const token_expired = (state) => {
    return state.token_expired;
}